alert('Kindly Login to access the site')   //the user to sign up

var loginID = prompt('enter ID')          //get the username
var loginPass = prompt('enter password')  //get the password

var loginID = 'jsclass';        //set the defaultname
var loginPass = 'learning102';  //set the defaultpassword

if ((username === Loginname) && (loginPass === loginPass))      //check the condition and give access if they correspond
alert('if u have  a diff')